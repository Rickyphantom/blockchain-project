// app/market/page.tsx
export default function MarketPage() {
  return (
    <section className="card">
      <h2 style={{marginTop:0}}>마켓</h2>
      <p style={{opacity:.9}}>여기에 문서 조회(getDocument) / 구매(buy) UI를 붙일 예정입니다.</p>
    </section>
  );
}
