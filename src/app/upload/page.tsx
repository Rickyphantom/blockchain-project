// app/upload/page.tsx
export default function UploadPage() {
  return (
    <section className="card">
      <h2 style={{marginTop:0}}>문서 등록 (판매)</h2>
      <p style={{opacity:.9}}>여기에 IPFS 업로드 + CID 입력 + 가격 설정 + 컨트랙트 listDocument 연동을 붙일 예정입니다.</p>
    </section>
  );
}
